/*angular.module('trialBalance', [])
.controller('TrialBalanceController', ['$scope', function($scope) {

  this.valueHour = 90;

  this.init = function init(valueHour, totalHours) {
  	this.valueHour = valueHour;
  	this.totalHours = totalHours;
  	this.calcTotal();
  	// $scope.$apply();
  };

  this.calcTotal = function calcTotal() {
    this.totalValue = this.totalHours * this.valueHour;
  };
}]);*/